import React from 'react'; import { Clock3 } from 'lucide-react'; import type { Commit } from '../types';
export function CommitTimeline({ commits, onSelect }: { commits: Commit[]; onSelect: (hash: string) => void }) { return <section className="min-w-0 rounded-[var(--control-radius)] border border-border bg-surface-raised"><div className="flex items-center gap-2 border-b border-border px-3 py-2 text-sm font-semibold"><Clock3 size={15} className="text-accent" />Commits</div><div className="px-3">{commits.map((commit, i) => <button type="button" data-commit-hash={commit.hash} key={commit.hash} onClick={() => onSelect(commit.hash)} className="flex min-h-11 w-full cursor-pointer gap-2 py-2 text-left text-[11px] hover:bg-surface-sunken/40"><span className={i === 0 ? 'text-accent' : 'text-text-subtle'}>●</span><span className="min-w-0"><span className="block truncate text-text">{commit.subject} {commit.merge || (commit.parents?.length ?? 0) > 1 ? '· merge' : ''}</span><span className="mt-0.5 flex flex-wrap gap-2 text-[10px] text-text-muted"><span className="font-mono text-accent">{commit.shortHash ?? commit.hash.slice(0, 7)}</span><span>{commit.author}</span><span>{commit.authoredAt ?? commit.date}</span>{commit.refs && <span>refs: {Array.isArray(commit.refs) ? commit.refs.join(', ') : commit.refs}</span>}{commit.parents && <span>parents: {commit.parents.length}</span>}</span></span></button>)}</div></section>; }
